CREATE DATABASE IF NOT EXISTS peak_transformation CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE peak_transformation;
-- The Flask app creates/updates the daily_logs table automatically on startup.
